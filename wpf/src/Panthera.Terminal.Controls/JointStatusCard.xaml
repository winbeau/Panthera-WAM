<UserControl x:Class="Panthera.Terminal.Controls.JointStatusCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="170" MinHeight="70">
    <Border Padding="9,7" Style="{DynamicResource CockpitSubtlePanelStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontFamily="Cascadia Mono, Consolas" FontSize="12" FontWeight="Bold" />
                <TextBlock Grid.Column="1" Margin="7,1,0,0"
                           Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="9" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="{Binding Position, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:+0.000;-0.000;0.000}}"
                               FontFamily="Cascadia Mono, Consolas" FontSize="11" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                    <Ellipse Width="6" Height="6" Margin="7,0,0,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOnline, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsWarning, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </StackPanel>
            </Grid>
            <ProgressBar Grid.Row="1" Height="5" Margin="0,7,0,0" Maximum="100"
                         Value="{Binding PositionPercent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Grid Grid.Row="2" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:F1}}"
                           FontFamily="Cascadia Mono, Consolas" FontSize="8" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <TextBlock Grid.Column="1" Text="{Binding Velocity, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}v {0:F2} rad/s}"
                           FontFamily="Cascadia Mono, Consolas" FontSize="8" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <TextBlock Grid.Column="2" HorizontalAlignment="Right"
                           Text="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:F1}}"
                           FontFamily="Cascadia Mono, Consolas" FontSize="8" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
